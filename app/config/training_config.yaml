num_epochs: 1000
batch_size_train: 64
batch_size_val: 64
train_test_split: 0.8
max_nb_steps: 
learning_rate: 0.001
training_pathway: p2p
device: cuda #cpu #cuda
save_every: 5
model_artifacts_dir: 'src/domain/model'
use_wandb: true
weight_decay: 0.1
model_id: 