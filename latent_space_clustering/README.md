#  Latent Space Clustering and Visualization Library

## Overview
The Latent Space Clustering and Visualization Library is a Python library designed to cluster and visualize latent space representations of a dataset using a Gaussian Mixture Model (For now. Likely will extend this in the future). The library provides a modular and extensible framework that allows developers to easily integrate clustering and visualization capabilities into their machine learning projects.